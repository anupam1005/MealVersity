# MealVersity Backend

This folder is reserved for the MealVersity backend API server.

## Planned Stack
- **Runtime**: Node.js (Express or Fastify)
- **Database**: PostgreSQL / MongoDB
- **Authentication**: JWT
- **Storage**: AWS S3 / Cloudinary

## Planned Endpoints
- `POST /auth/login` - User login
- `POST /auth/register` - User registration
- `GET /restaurants` - List restaurants
- `GET /restaurants/:id/menu` - Restaurant menu
- `POST /orders` - Place an order
- `GET /orders/:id` - Get order status
- `GET /subscriptions` - List subscriptions
- `POST /subscriptions` - Create subscription
- `GET /profile` - User profile
- `PUT /profile` - Update profile

## Getting Started
Backend not yet implemented. All data is currently mocked in the Frontend (Expo) app.
